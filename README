
//npm i -g heroku
//heroku -v
//heroku login

//git --version
//git init -> make .git folder 
//git status -> show the status of the project ' what is untracked, tracked, committed 
//git add . -> add everyThing to be tracked  ; add to staged area to be able to commit it
//git add file/folderName
//git commit -m "Message" -> add tracked files to the repo with a specific comment/message
//git remote -> show remote connection like 'heroku & branch master'

//gitbash recommended
// ssh-keygen -t rsa -b 4096 -C "moatasemsalama954@yahoo.com"
// ls -a -l ~/.ssh
//eval $(ssh-agent -s)
//ssh-add -K ~/.ssh/id_rsa
//cat ~/.ssh/id_rsa.pub -> to get SSH key and put it to github

//git remote add origin git@github.com:Mo3eto/weather-website.git -> connect local repo 'on the device' with online repo
//git push -u origin master
//
//ssh -T git@github.com -> to test connection with github

//heroku keys:add -> add SSH key to heroku 
//heroku create moa-weather-website 
//heroku config:set key=value -> to setup your env variables
//heroku config:unset key
//git push heroku master -> to deploy the code to heroku server


//C:\Users\moastem\mongodb\bin\mongod.exe --dbpath="F:\NodeProjects\mongodb-data"
